name: usb_voice_typer
description: Simple USB Voice to PC Typer
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  speech_to_text: ^7.0.0
  http: ^1.2.0
  permission_handler: ^11.3.0
  shared_preferences: ^2.3.0
  system_alert_window: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
